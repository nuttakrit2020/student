const fs = require('fs'); let code = fs.readFileSync('src/app/admin/page.js', 'utf8'); code = code.replace(/const rooms = useMemo.*?\[summaryData\]\);/s, 'const rooms = [...new Set(summaryData.map(s => s.student.room || \\'\\').filter(Boolean))].sort();'); code = code.replace(/const filteredStudents = useMemo.*?\[summaryData, filterRoom\]\);/s, 'const filteredStudents = filterRoom ? summaryData.filter(s => s.student.room === filterRoom) : summaryData;'); fs.writeFileSync('src/app/admin/page.js', code);
